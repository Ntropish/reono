// Global type declarations for tests
declare global {
  var TEST_PORT: number;
  var TEST_BASE_URL: string;
  var TEST_API_KEYS: {
    ADMIN: string;
    USER: string;
    PREMIUM: string;
    INVALID: string;
  };
}

export {};
